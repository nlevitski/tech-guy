# Компьютерщик

Static Next.js landing page for `компьютерщик.бел`.

## Commands

```bash
bun install
bun run dev
bun run lint
bun run build
docker compose up --build
```

The app listens on port `3015`.
